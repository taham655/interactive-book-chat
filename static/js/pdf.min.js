// Due to size limitations, this would normally contain the minified PDF.js code.
// In production, you should use the CDN version:
// https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js

// This is a minimal wrapper to ensure PDF.js is properly configured
if (typeof window !== 'undefined' && 'pdfjsLib' in window) {
    window.pdfjsLib.GlobalWorkerOptions.workerSrc = '/static/js/pdf.worker.min.js';
}

// Initialize PDF.js
const pdfjsLib = window['pdfjs-dist/build/pdf'];
pdfjsLib.GlobalWorkerOptions.workerSrc = '/static/js/pdf.worker.min.js';

// Export the PDF.js functionality
window.PDFLib = pdfjsLib;
